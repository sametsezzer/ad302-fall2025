Phase 1 — Data Collection (generate_content.py + phones.csv)

You are building a data pipeline. I have a file called sites.csv containing 20 website URLs (one per line). 

Your task: write a Python script called `generate_content.py` that does the following:

1. Reads all 20 URLs from sites.csv
2. Uses the OpenAI API (model: "gpt-4o-search-preview" with web search enabled) to find the contact phone number for each website. For each URL, send a prompt like: "Visit this website and find the contact phone number: <url>. Return only the phone number or 'Not found' if unavailable."
3. Saves results to phones.csv with two columns: url, phone
4. Uses the OpenAI API again (with web search) to research the FIRST website in the list (https://www.atelierboho.com.tr/) — find out what the business does, their products/services, brand story, and any notable info. Write a clear 3-4 sentence English summary.
5. Saves that summary to content.txt

Requirements:
- Use asyncio + aiohttp or ThreadPoolExecutor to run all 20 phone lookups in PARALLEL (minimize total runtime)
- Use environment variable OPENAI_API_KEY for the API key
- Use openai Python SDK v1+
- Handle errors gracefully (if a site fails, write "Not found")
- Print progress to console

The sites.csv file contains these URLs:
https://www.atelierboho.com.tr/
https://www.fatamorgana.com.tr/
https://www.ipeksilver.com/
https://www.latikagumus.com
https://www.meidesign.co/
https://www.melissa.com.tr/
https://www.meysadesiign.com/
https://www.monsternotebook.com.tr/
https://www.mrstickercustoms.com/tr
https://www.necoaksesuar.com/hizmetler
https://www.niposinta.com/
https://www.roxelana.com.tr/1/
https://www.tomrisjewellery.com/
https://www.unikejewellery.com/en/jo/glow-circle-earrings_p716.html
https://mylamp.com.tr/
https://mysummerblue.com.tr
https://neom-jewelry.com/
https://nevjewellery.com/
https://www.bonetasarim.com/
https://www.bygolden.com/

Create generate_content.py and also create the sites.csv file with the above URLs.

Phase 2 — Web App (app/)

Now build a minimal local web app inside the `app/` folder. Use Node.js + Express (or a plain HTML file served via a simple Express server).

Requirements:
1. Single page UI with a clean, modern design
2. The page reads content.txt (the summary of the first website) and displays it prominently on screen
3. Two buttons below the summary:
   - Button 1: "Visit Atelier Boho" → opens https://www.atelierboho.com.tr/ in a new tab
   - Button 2: "Visit Alight Apps" → opens https://www.alightapps.com/ in a new tab
4. The app should serve the content.txt from the backend (Express reads the file and sends it as JSON to the frontend)
5. Style it simply but professionally — centered layout, readable font, clear button styling

File structure:
- app/server.js (Express server, reads ../content.txt)
- app/public/index.html (frontend with fetch call to get the summary)
- package.json in the app/ folder

The server should run on port 3000. Create all files now.

Phase 3 — Final Check & README

Now do a final review, wire everything together, and push to a public GitHub repo.

─── STEP 1: Verify file structure ───
Make sure these files exist at the root level:
- sites.csv
- generate_content.py
- phones.csv (generated by the script)
- content.txt (generated by the script)
- package.json (root level, can be minimal)
- app/ folder containing the web app
- README.md

─── STEP 2: Create root-level package.json ───
Create a minimal package.json at the root (not inside app/) like:
{
  "name": "ai-pipeline-project",
  "version": "1.0.0",
  "description": "AI-powered data pipeline and web app",
  "scripts": {
    "start": "cd app && node server.js"
  }
}

─── STEP 3: Create README.md ───
Create README.md at the root with the following content:

# AI Pipeline Project

## Overview
End-to-end pipeline: CSV input → OpenAI web search → output files → local web app.

## Project Structure